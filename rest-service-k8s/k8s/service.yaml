# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: rest-service
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
  selector:
    app: rest-service
